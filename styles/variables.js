import { css } from 'styled-components'

const variables = css`
    :root{
        /* --font-sans: 'cooper-black-std', -apple-system, system-ui, sans-serif;
        --font-sans: 'cooper-black-std', -apple-system, system-ui, serif; */
    }`